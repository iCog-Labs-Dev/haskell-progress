factorial :: Int -> Int
factorial x = product [1..x]

squaredSum :: Int -> Int -> Int
squaredSum x y = x * x + y * y

circleArea :: Float -> Float
circleArea radius = 2 * pi * radius

circumference :: Float -> Float
circumference radius = pi * radius * radius
